import { isLeap } from "./leap-year";

xdescribe('LeapYear', () => {
    test('isLeap should returns xxx when year is xxx', () => {
        // Arrange
        // Act
        // Assert
        expect(false).toBe(true)
    })
})
